import {mkdir,readFile,writeFile,rename} from 'node:fs/promises';import path from 'node:path';
export async function readJson(file,fallback){try{return JSON.parse(await readFile(file,'utf8'))}catch(e){if(e.code==='ENOENT')return fallback;throw e}}
export async function writeJson(file,value){await mkdir(path.dirname(file),{recursive:true});const temp=file+'.tmp';await writeFile(temp,JSON.stringify(value,null,2)+'\n');await rename(temp,file)}
export function costUsd(response){const u=response.usage||{};const input=Number(u.input_tokens)||0,cached=Number(u.input_tokens_details?.cached_tokens)||0,output=Number(u.output_tokens)||0;const calls=(response.output||[]).filter(o=>o.type==='web_search_call').length;return (input-cached)*.75/1e6+cached*.075/1e6+output*4.5/1e6+calls*.01;}
export function reserveBudget(ledger,amount,limit,now=new Date()){const month=now.toISOString().slice(0,7);const current=ledger.month===month?ledger:{month,spent:0,reserved:0,calls:[]};if(current.spent+current.reserved+amount>limit)throw Error('MONTHLY_BUDGET_REACHED');return {...current,reserved:current.reserved+amount};}
